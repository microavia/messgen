{
  "name": "messgenjs",
  "version": "1.0.0",
  "description": "",
  "main": "src/messgen.js",
  "type": "module",
  "scripts": {
    "build": "vite build",
    "test": "vitest",
    "prettier": "prettier --write  \"src/**/*.js\" \"tests/**/*.js\" \"*.js\"",
    "gen": "python3 ../../messgen-generate.py --basedir ../../tests/messages --protocol messgen/test_proto --protocol messgen/messgen_test --protocol another_proto --outdir ./tests",
    "gen-json": "npm run gen -- --lang json",
    "gen-ts": "npm run gen-json && npm run gen -- --lang ts",
    "gen-cpp": "npm run gen -- --lang cpp",
    "gen-md": "npm run gen -- --lang md",
    "benchmark": "vitest bench"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@rollup/plugin-terser": "^0.4.4",
    "prettier": "3.2.5",
    "tinybench": "^2.7.0",
    "vite": "^5.2.8",
    "vitest": "^1.5.0"
  }
}
