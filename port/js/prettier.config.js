/** @type {import("prettier").Config} */
const config = {
    tabWidth: 4,
    useTabs: false,
    singleQuote: true,
    printWidth: 120,
    trailingComma: 'none',
    semi: false
}
module.exports = config
